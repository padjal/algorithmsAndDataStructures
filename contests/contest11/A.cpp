/*
 * This program finds the minimum spanning tree.
 */

#include <iostream>

class Edge {

};

int main() {
    // User input.
    int n, m;
    std::cin >> n >> m;
    // Check input.
    if(n < 2 || n > 20000 || m < 1 || m > 100000){
        return 0;
    }

    return 0;
}
